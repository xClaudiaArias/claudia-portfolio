import React from 'react'; 
import styled from 'styled-components';

const HomeContainer = styled.div`
    position: relative;
    z-index: 1;
    min-height: 80vh; 
    text-align: center;
    background: none;
`;

const Home = () => {

    return (
        <HomeContainer>

        </HomeContainer>
    );
}

export default Home;
